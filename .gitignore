# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# SQLite databases (generated, not source of truth in the repo).
# Note: only match the DB files themselves — a bare `data/` rule would also
# swallow source dirs like packages/frontend/src/data.
# Also covers `./data/cache/` (raw Jira sync dumps — never commit; use
# `npm run export:obfuscated` to produce a shareable anonymized fixture).
*.db
*.db-shm
*.db-wal
/data/
/packages/*/data/

# Test / coverage
coverage/
test-results/
playwright-report/
.playwright/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env
.env
.env.local
